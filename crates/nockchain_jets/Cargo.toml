[package]
name = "nockchain_jets"
version = "0.1.0"
edition = "2021"

[lib]
name = "nockchain_jets"
path = "src/lib.rs"
crate-type = ["cdylib", "rlib"]

[dependencies]
zkvm-jetpack = { path = "../zkvm-jetpack" }
